apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: onaimhub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  
  storageClassName: local-path

